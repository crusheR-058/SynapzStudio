node_modules
dist
dist-ssr
*.local
.vite
.DS_Store
.screenshots
bin/
server/data.json

# Desktop app: installer output + generated icons (regenerated by `npm run icons`)
release/
build/

.vercel

# Local environment files with secrets (keep .env.example tracked)
.env
.env.local
.env.*.local
