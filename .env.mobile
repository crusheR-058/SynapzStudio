# Loaded only by `vite build --mode mobile` (the Capacitor app build). The mobile
# app runs from a local origin, so backend calls must hit the hosted Vercel URL.
# (Public value — safe to commit.)
VITE_API_BASE=https://synapz-music.vercel.app
